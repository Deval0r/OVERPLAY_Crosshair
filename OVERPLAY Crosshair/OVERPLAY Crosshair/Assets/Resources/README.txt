Resources Directory

This directory is for assets that need to be loaded at runtime using Resources.Load().

Currently, this directory is empty as the application doesn't require runtime asset loading.

If you need to load assets dynamically (e.g., crosshair presets, textures, etc.), you can place them here and use Resources.Load() to access them.
